-- liste les lignes de la table second_table de la base de données active.
-- en affichant dans la première colonne le score et dans la deuxième le nom,
-- triées par score décroissant du score.
SELECT score, name FROM second_table ORDER BY score DESC;
